import { UpdateButtonPipe } from './update-button.pipe';

describe('UpdateButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
