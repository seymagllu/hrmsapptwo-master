import { VerifyButtonPipe } from './verify-button.pipe';

describe('VerifyButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifyButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
