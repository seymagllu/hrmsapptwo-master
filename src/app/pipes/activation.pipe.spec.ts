import { ActivationPipe } from './activation.pipe';

describe('ActivationPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivationPipe();
    expect(pipe).toBeTruthy();
  });
});
