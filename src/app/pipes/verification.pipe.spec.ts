import { VerificationPipe } from './verification.pipe';

describe('VerificationPipe', () => {
  it('create an instance', () => {
    const pipe = new VerificationPipe();
    expect(pipe).toBeTruthy();
  });
});
