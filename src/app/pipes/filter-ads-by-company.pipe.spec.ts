import { FilterAdsByCompanyPipe } from './filter-ads-by-company.pipe';

describe('FilterAdsByCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdsByCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
